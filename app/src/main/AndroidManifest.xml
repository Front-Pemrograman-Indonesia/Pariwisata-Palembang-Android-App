<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ourshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OurShop">
        <activity android:name=".activities.ActivityKuliner" />
        <activity android:name=".activities.ActivityDetailKuliner" />
        <activity android:name=".activities.ActivityPenginapan" />
        <activity android:name=".activities.ActivityDetailPenginapan" />
        <activity android:name=".activities.ActivityWisata" />
        <activity android:name=".activities.ActivityDetailWisata" />
        <activity android:name=".activities.ActivityTempatIbadah" />
        <activity android:name=".activities.ActivityDetailTempatIbadah" />
        <activity
            android:name=".activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>